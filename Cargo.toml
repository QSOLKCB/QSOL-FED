[package]
name = "qsol-fed"
version = "0.1.0"
edition = "2024"
description = "Reference constitutional core for the QSOL-FED sovereign federation protocol"
license = "Apache-2.0"
repository = "https://github.com/QSOLKCB/QSOL-FED"
rust-version = "1.85"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
